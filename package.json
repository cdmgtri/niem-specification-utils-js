{
  "name": "niem-specification-utils",
  "version": "1.0.0",
  "description": "Extracts rule and definition information from NIEM specifications.",
  "main": "./src/index.js",
  "bin": {
    "cli": "bin/index.js"
  },
  "scripts": {
    "build": "node bin/index.js",
    "build:debug": "cross-env DEBUG=niem npm run build",
    "ci": "npm run test:cov && npm run coverage",
    "coverage": "coveralls < coverage/lcov.info",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.5",
    "cross-env": "^7.0.3",
    "debug": "^4.3.1",
    "fs-extra": "^9.1.0",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "coveralls": "^3.1.0",
    "jest": "^26.6.3"
  }
}
